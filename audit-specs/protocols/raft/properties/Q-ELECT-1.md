# Q-ELECT-1 — Election safety

For every fixed voter set `C` and term `t`, at most one distinct candidate satisfies `ElectionWon(c,t,C)`.

`ElectionWon` denotes an implementation-completed election decision as defined in `EVENT_SEMANTICS.md`.
